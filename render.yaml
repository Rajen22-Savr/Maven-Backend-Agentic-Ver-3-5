
services:
  - type: web
    name: maven-backend-v3-5
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn maven_api_backend:app --host 0.0.0.0 --port 10000
    plan: free
    autoDeploy: true
    rootDir: v3_5
